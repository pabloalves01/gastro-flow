// arquivo que configura o caminho que as migrations, models e seeders serao criados

const path = require('path');

module.exports = {
  'config': path.resolve('src/config/config.json'), // Caminho para o arquivo de configuração
  'models-path': path.resolve('src/models'),        // Caminho para os models
  'migrations-path': path.resolve('src/migrations'), // Caminho para as migrations
  'seeders-path': path.resolve('src/seeders'),      // Caminho para os seeders, se houver
};
